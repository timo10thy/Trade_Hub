# TradeHub Backend
Default port: 8000
If port 8000 is in use on your machine, change the port mapping 
in docker-compose.yml:
  ports:
    - "8001:8000"  # change 8001 to any free port on your machine